apiVersion: v1
kind: Service
metadata:
  labels:
    app: vaccine-traceability-webapp
  name: vaccine-traceability-webapp
spec:
  ports:
    - name: '3000'
      port: 3000
      targetPort: 3000
  selector:
    app: vaccine-traceability-webapp
